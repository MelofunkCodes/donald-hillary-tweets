{
  "name": "test-heroku-tweet",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "heroku-start": "heroku local",
    "start:client": "cd client && npm start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "morgan": "^1.9.0",
    "npm-run-all": "^4.1.2",
    "path": "^0.12.7",
    "twitter": "^1.7.1"
  }
}
